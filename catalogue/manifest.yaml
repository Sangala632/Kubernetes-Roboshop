apiVersion: v1
kind: ConfigMap
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    component: catalouge
    project: roboshop
    tire: application
data:
  MONGO: "true" 
  MONGO_URL: "mongodb://mongodb:27017/catalogue"
---
apiVersion: apps/v1
kind: deployment
metadata:
  name: catalogue
  namespace: roboshop
  # deployment labels
  labels:
    component: catalogue
    project: roboshop
    tire: application
spec:
  replicas: 1   # These are the labels replica set use to create pod replicas, this should match pod labels
  selector:
    matchLabels:
      component: catalogue
      project: roboshop
      tire: application
    # This is pod definition
    template:
      metadata:
        labels:
          component: catalogue
          project: roboshop
          tire: application
  spec:
    containers:
      - name: catalogue
        image: sangala2025/catalogue:v1
    envFrom:
      - configMapRef:
          name: catalogue
---
apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop

  labels: 
    component: catalogue
    project: roboshop 
    tire: application
spec:
  selectors:
    component: catalogue
    project: roboshop 
    tire: application
  ports:
    - protocol: TCP
      port: 27017 #service port
      targetPort: 27017 #container port
